<!-- record-meta
{
  "record_id": "20260914-031931-7e4a21a",
  "experiment": "reset-pin-through",
  "supersedes": null,
  "git_revision": "7e4a21a",
  "provenance": {
    "klt_version": "0.4.0+g5c757081680e",
    "pdk": { "name": "n/a", "version": "n/a" },
    "deck": { "content_hash": "n/a" },
    "inputs": [
      { "path": "src/tt_um_2amlogic_protocol_emulator.v", "content_hash": "sha256:4945013ad813d88c92b20beb8f959d44080fe7b009d6ca55edd32b34ffd15708" },
      { "path": "verification/test_protocol_emulator.py", "content_hash": "sha256:0cc707de06290f5fc6df4b61521a531fc75e6baab204e6e406a018bc439d682f" },
      { "path": "verification/_dut.py", "content_hash": "sha256:8e89b02ff7bdafac12f1213a3142ec3e8a8ff95ca2a5e5772d271e74040ec0af" },
      { "path": "verification/request-protocol-emulator.json", "content_hash": "sha256:bc5895af72f6be5202c7c90b606da1f99046c08b1acf5bfefb02cd36a127cff1" }
    ]
  }
}
-->

- **Record ID**: 20260914-031931-7e4a21a
- **Claim**: `src/tt_um_2amlogic_protocol_emulator.v` (the harness-bootstrap stub top, issue #2) resets its registered output to zero and registers `ui_in` onto `uo_out` on every clock edge while `ena` is high -- the "First test: the stub top's reset and pin-through behavior" `verification/README.md` calls for. This is this program's own klt-driven cocotb bench, distinct from (but checking the same behavior as) the Tiny Tapeout template's own `test/test.py`.
- **Design provenance**: `rtl` (`src/tt_um_2amlogic_protocol_emulator.v` @ `7e4a21a`; see `rtl/README.md` for why the top lives under `src/` rather than `rtl/`).
- **Run configuration**: `klt functional-verification verification/request-protocol-emulator.json --format json`, engine `icarus` (Icarus Verilog 12.0), cocotb 2.1.0, `random_seed: 1`, `timescale: ["1ns", "1ps"]`, 3 directed test cases (`test_reset_clears_output`, `test_pin_through`, `test_ena_gates_updates`).
- **Statistical convention**: N/A -- directed tests only, no randomization (this stub has no protocol logic to constrained-random test yet).
- **Result**: PASS. All 3/3 tests passed (0 failed, 0 skipped). See `artifacts/20260914-031931-7e4a21a/klt-functional-verification.json` for the raw envelope and `results_icarus.xml` for the per-test JUnit report.
- **klt provenance**: `klt` 0.4.0+g5c757081680e (`2AMLogic/klayout-tools` @ `5c75708`) drove this run via `klt functional-verification`. This is a locally-rebuilt `klt` with `cocotb`/`cocotb-tools` added (`uv tool install --with cocotb --with cocotb-tools`) -- the repo's persistent `klt` install (per `layout/toolchain.json`, scoped to the `layout/` DRC/LVS flow only) has no `cocotb` in its isolated tool venv, so a bare `klt functional-verification` fails with `cocotb is not installed`. No PDK dependency: `pdk`/`deck` are `n/a`, per this convention's rule for Icarus/cocotb-only runs (see `verification/README.md`, copied from `2AMLogic/sky130-modexp`).
- **Links**: driver/testbench: `verification/test_protocol_emulator.py`, `verification/_dut.py`; request: `verification/request-protocol-emulator.json`; design under test: `src/tt_um_2amlogic_protocol_emulator.v`; Tiny Tapeout template's own equivalent bench: `test/test.py`; artifacts: `artifacts/20260914-031931-7e4a21a/`.
- **Timestamp / author**: 2026-09-14T03:19:31Z, Loom Builder (agent), issue #2.
- **Supersedes**: none.
