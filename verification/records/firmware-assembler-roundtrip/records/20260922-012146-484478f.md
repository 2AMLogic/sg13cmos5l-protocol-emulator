<!-- record-meta
{
  "record_id": "20260922-012146-484478f",
  "experiment": "firmware-assembler-roundtrip",
  "supersedes": null,
  "git_revision": "484478f",
  "provenance": {
    "klt_version": "0.4.0+g5c757081680e.dirty",
    "pdk": { "name": "n/a", "version": "n/a" },
    "deck": { "content_hash": "n/a" },
    "inputs": [
      { "path": "firmware/tools/asm.py", "content_hash": "sha256:605fd39156e3eabbbe1fadec3c560b28e1578f192e24c7c75ff8c3054a0c4fe0" },
      { "path": "firmware/asm/demo_roundtrip.asm", "content_hash": "sha256:bca48b7236b2912ffb3c23ef23c8bec1e070afd823f4a837fbed282cc8f4473c" },
      { "path": "firmware/build/demo_roundtrip.hex", "content_hash": "sha256:98ea229e39b27a8ecb1405c1cdb0ef0e0eeec617c5d63bed264a501ebd0b39ec" },
      { "path": "firmware/build/demo_roundtrip.cycles.txt", "content_hash": "sha256:22ef92b05876ff436e2e855a87174e9364317c0aa3b8916a7109bf52439c740a" },
      { "path": "verification/test_firmware_roundtrip.py", "content_hash": "sha256:9d1959d91de1e29e9bab7e1c74402843035a41e53d93505d1d1bb24072027a9f" },
      { "path": "verification/request-firmware-roundtrip.json", "content_hash": "sha256:09d3f6cd5af1daa9deffcf966c68af4f5ddfe30824ccf2ff8caf059a3ba406bc" },
      { "path": "rtl/protocol_program_memory.v", "content_hash": "sha256:c7f989cfff1d45f40c110ffa4a29aae8c0bd876be47af943b6e53001cb38e4d1" }
    ]
  }
}
-->

- **Record ID**: 20260922-012146-484478f
- **Claim**: The DR 0003 firmware toolchain exists per its ratified fixed design (issue #22, `spec/decision-records/0003-firmware-toolchain.md`): `firmware/tools/asm.py` encodes exactly DR 0001's ratified opcode table (`spec/decision-records/0001-isa.md` section "Encoding" — cited, not re-derived), and its output agrees with the RTL: a program assembled by it (`firmware/asm/demo_roundtrip.asm`, exercising all 16 opcodes) serially loads through `rtl/protocol_program_memory.v`'s DR 0001 load phase and reads back word-for-word through the fetch port the ISA core will execute from — the assembler and the RTL agreeing, not either alone. Until the core datapath (issue #18, open) lands, "executes correctly in the cocotb bench" is scoped to this program-memory path, the same out-of-band readback stance as the `program-load-phase` record (DR 0001 defines no readback instruction; the 16-opcode table is exactly full).
- **Design provenance**: `rtl` (`rtl/protocol_program_memory.v` @ `484478f`, unchanged from the `program-load-phase` record's citation) plus the toolchain itself (`firmware/tools/asm.py` @ `484478f`, its committed source and build artifacts hashed above).
- **Run configuration**: cocotb leg `klt functional-verification verification/request-firmware-roundtrip.json --format json` (the pristine-install klt at the `layout/toolchain.json` pin, Icarus Verilog 13.0, cocotb 2.1.0 inside klt's environment, `random_seed: 1`, `timescale: ["1ns","1ps"]`, a 10 ns simulation clock, 3 directed tests). Assembler unit-test leg: `python3 firmware/tools/test_asm.py` (34 self-managed cases) and `python3 firmware/tools/asm.py firmware/asm/demo_roundtrip.asm --check` (committed-artifact freshness), both run at the same tree.
- **Statistical convention**: N/A — directed tests only. The demo program, its committed image, and its cycle report are deterministic by construction (no timestamps, canonical repo-relative source path, fixed-in-source patterns), so every run is byte-reproducible.
- **Result**: **PASS.** cocotb leg 3/3 passed (per-test statuses in `artifacts/20260922-012146-484478f/results_icarus.xml`): (1) re-assembling the committed source reproduces both committed `firmware/build/` artifacts byte-for-byte and the demo program is lint-clean; (2) the 27-word committed image loads through the serial load phase cycle-exactly (1 mode-sampling + 16×27 bit + 1 MODE-drop exit edge = 434 edges asserted) and every word reads back exactly through the fetch port at addresses 0..26; (3) the committed cycle-accounting report (`WORDS 27`, `TOTAL-CYCLES 30`, `CYCLES name=banner start=1 end=5 cycles=8`) agrees with cycle arithmetic recomputed independently from the image words (opcode 0xB stalls imm8+1; all else 1 cycle per DR 0001's table). Assembler unit-test leg 34/34 passed: every one of the 16 opcode rows encoded against hand-derived expected words, label/absolute-address resolution, WAIT cycle math (WAIT 0 = 1 cycle through WAIT 255 = 256), `.cyclesec` exact entry→exit accounting, deterministic byte-reproducible artifact rendering, the DR 0001-delegated data-dependent-latency lint (warns on a conditional branch over `IN`-sampled flags; silent for assemble-time-constant trip counts; `LDI` clears taint; `MOV` propagates it), and 20 malformed-input classes each raising a line-numbered error (unknown mnemonic, arity, register range, immediate range/negative, port/direction typos, `IN`-from-write-only / `OUT`-to-read-only direction bugs, undefined/duplicate labels, >256-word programs, all `.cyclesec` misuses). `--check` reported the committed artifacts fresh.
- **klt provenance**: Driven by the `layout/toolchain.json`-pinned klt, installed exactly as `scripts/setup-env.sh` provisions it (`uv tool install --force --with cocotb==2.1.0 --with cocotb-tools==0.1.0 "klayout-tools @ 5c757081680e207b6abbc7a6ca7d3688ad07072a"`); the binary self-reports `0.4.0+g5c757081680e.dirty` (frozen in `artifacts/20260922-012146-484478f/klt-version.txt`), the same pin the `program-load-phase` and `reset-pin-through` records cite. This run has no PDK dependency — an Icarus-only cocotb run never touches a standard-cell library — so `pdk` and `deck` are `n/a` per the convention (the same as the `reset-pin-through` record's Icarus-only legs).
- **Links**: assembler: `firmware/tools/asm.py`; its unit tests: `firmware/tools/test_asm.py`; source program: `firmware/asm/demo_roundtrip.asm`; committed build artifacts: `firmware/build/demo_roundtrip.hex`, `firmware/build/demo_roundtrip.cycles.txt`; driver/testbench: `verification/test_firmware_roundtrip.py`; request: `verification/request-firmware-roundtrip.json`; design under test: `rtl/protocol_program_memory.v`; cold-start invocation: `firmware/README.md`; authoritative designs: `spec/decision-records/0003-firmware-toolchain.md`, `spec/decision-records/0001-isa.md`; artifacts: `artifacts/20260922-012146-484478f/`.
- **Timestamp / author**: 2026-09-22T01:21:46Z, Loom Builder (agent), issue #22.
- **Supersedes**: none.
