<!-- record-meta
{
  "record_id": "20260922-012402-420c370",
  "experiment": "die-area-baseline",
  "supersedes": null,
  "git_revision": "420c370994deff79f18849113b0d2a6985d47715",
  "provenance": {
    "klt_version": "0.5.0+g1d964cf4ed93",
    "pdk": {
      "name": "ihp-sg13cmos5l",
      "version": "unknown (local PDK install reports no version stamp -- `klt pdk find --pdk ihp-sg13cmos5l` reports `version: -`)"
    },
    "deck": {
      "name": "sg13cmos5l_stdcell_typ_1p20V_25C",
      "content_hash": "sha256:34163f3e7de9f854afac57332ba06f1739b78e7ac35660a8a5ddd39c570124ad"
    },
    "inputs": [
      { "path": "rtl/protocol_program_memory.v", "content_hash": "sha256:c7f989cfff1d45f40c110ffa4a29aae8c0bd876be47af943b6e53001cb38e4d1" },
      { "path": "src/tt_um_2amlogic_protocol_emulator.v", "content_hash": "sha256:4945013ad813d88c92b20beb8f959d44080fe7b009d6ca55edd32b34ffd15708" },
      { "path": "flow/synthesize-program-memory.json", "content_hash": "sha256:1027d597f33e037767070c97e9964b512f219ee7d80a2ed8883cebecc256b6db" },
      { "path": "flow/synthesize-protocol-emulator.json", "content_hash": "sha256:933944338c50e8233f79b71e04fed78afe14e0c89eb1c2b5fcb16f85fbb22fff" },
      { "path": "flow/run-die-area-par.sh", "content_hash": "sha256:0470171a6d4da7e1f03fc093b8e4b1bdbaa01667d15dac86af6ebabef15f929a" },
      { "path": "layout/toolchain.json", "content_hash": "sha256:4c626bd79932dd23ae9a81fe4d4b691c18ca229ac573dd1e33378d54d151f598" }
    ]
  }
}
-->

- **Record ID**: 20260922-012402-420c370
- **Claim**: issue #21 — target-spec row 7's open half: **no run of either flow had ever reported a die-area or utilization number** (only cell counts and liberty cell-area sums). This record supplies the first one: a `klt place-and-route` (OpenROAD) run that reaches floorplan **and legalized placement** for both synthesizable tops that exist today, compared against the ratified ≤2×2-tile budget with the tile pitch stated. It also records the outcome that comparison produces: **the ratified budget is not met — by the program memory alone, before the ISA core datapath (issue #18) adds a single cell** — exactly the "if the core does not fit, record it as a finding" branch of the issue's scope. Per that same scope ("the budget is a ratified target, not a suggestion"), `info.yaml`'s `tiles` is **not** grown to fit the measurement: the only defensible `tiles` value remains the stub's `1x1` (the actual submission top's die is 0.092 tiles, confirmed below), and the non-fit is recorded here and in `spec/gap-to-submission.md` row 7 instead. Flow attribution (CLAUDE.md two-flows rule): **this is the klt/Yosys/OpenROAD iteration flow's number** (DR 0002); the LibreLane leg still reports no die-area figure of its own, and the two are not interchanged.
- **Design provenance**: `layout` (post-floorplan, post-placement — no route) from `netlist` (post-synthesis, frozen in this record's artifacts) from `rtl` — two designs: `rtl/protocol_program_memory.v` @ `420c370` (the real RTL that exists; issue #19) and `src/tt_um_2amlogic_protocol_emulator.v` @ `420c370` (the harness-bootstrap stub, the current submission top). Both netlists were synthesized fresh by this run at the nominal corner (`typ_1p20V_25C`, liberty content hash `sha256:34163f3e…`, identical to the synthesis-baseline and sta-corner-sweep records' liberty) and the exact bytes each `klt place-and-route` run loaded are frozen under `artifacts/20260922-012402-420c370/`.
- **Run configuration**: `KLT_BIN=.venv/bin/klt ./flow/run-die-area-par.sh` from the repo root, with `PDK_ROOT` exported by the script (`/Users/rwalters/share/pdk` — required by the docker-wrapped `openroad`; see "klt provenance"). klt `0.5.0+g1d964cf4ed93` at `layout/toolchain.json`'s pin `1d964cf4ed93b15bd36acf1eb14e3855b3c500d1` (pin unchanged; this run is the live verification behind that file's 2026-09-22 paragraph growing `klt_required_commands` to `[drc, sta, place-and-route]`). Per design: `klt synthesize` at the existing recipe (`flow/synthesize-program-memory.json` / `flow/synthesize-protocol-emulator.json`, corner `typ_1p20V_25C`, clock 20 ns), then `klt place-and-route` with `target_stage: "place"` — floorplan method `utilization` at **40%** requested utilization, `aspect_ratio: 1.0`, `core_margin_um: 2.0`, `site: "CoreSite"` (the SITE both `sg13cmos5l_stdcell` LEFs declare), `io.layer_h/layer_v: Metal3/Metal2`, `constraints.clock_port: clk, clock_period_ns: 20.0`, `seed: 1`. Tile geometry (stated, from the template's own `info.yaml` per row 7's 2026-09-14 note, order of magnitude — no inter-tile spacing or margin is specified): **1 tile ≈ 167 × 108 µm = 18,036 µm²**; ≤2×2 budget = 72,144 µm²; competition maximum 8×4 = 577,152 µm².
- **Statistical convention**: N/A — one deterministic run per design (seed 1). Determinism spot-checked: the program memory's synthesis + floorplan + place was executed twice (an exploratory pass, then this runner) and both passes returned byte-identical figures (11,603 cells / 312,062.8896 µm²; die 787,434 µm²).
- **Result**: **MEASURED — and the measurement is a non-fit finding, not a pass.**
  - **Program memory (`rtl/protocol_program_memory.v`, the real RTL):** synthesis 11,603 cells / **312,062.8896 µm²** cell area (matching the row-6 note's figure exactly); `klt place-and-route` reached `stage_reached: "place"` — die area **787,434 µm²** (core 777,447 µm²), utilization 40.15% at floorplan / **41.92% at legalized placement**, 13,502 placed components (synthesis's 11,603 + ~16% placement/timing-repair buffers), engine `openroad 26Q3-2056-g41a28926b9`.
    - **Against the ≤2×2 budget (72,144 µm²):** the die is **10.91× over** (43.66 tile-equivalents); the *cell area alone* is **4.33× over** — the memory cannot fit 2×2 at **any** utilization (even 100% needs ≥18 tiles).
    - **Against the 8×4 competition maximum (577,152 µm²):** this die is **1.36× over**; the memory's cells alone would consume **54.1%** of the entire competition allocation — before the core datapath, before routing, before power delivery.
  - **Stub top (`src/tt_um_2amlogic_protocol_emulator.v`, the current submission top):** synthesis 16 cells / 537.0624 µm² (unchanged from the synthesis-baseline record); die area **1,651.61 µm²** (core 1,224.72 µm², placed utilization 43.85%) = **0.092 tiles** — `info.yaml`'s `tiles: "1x1"` is confirmed correct for the design actually being submitted today.
  - **The finding (recorded per the issue's third scope bullet):** the flip-flop program memory alone breaks the ratified ≤2×2 area budget by a factor of ~4.3 in cell area. This is not a placement-tuning artifact — it is structural to the storage implementation DR 0001 chose (256×16 flip-flop words). Consequences, recorded not acted on: (a) row 6's stretch item — a Tiny Tapeout SRAM macro in place of the flip-flop array — changes from "if area allows" to the load-bearing path for any ≤2×2 (or any sub-8×4) outcome; (b) alternatively the budget itself must be revised via the AREA-EFF convention, which requires a passing efficiency read at the proposed size *before* ratification — this record's numbers are the first input to that read, not a ratification. `info.yaml`'s `tiles` therefore stays `1x1` with its comment updated to cite this record (the issue's "set from it" resolves to: the measured number exceeds every valid `tiles` value for the memory, so the ratified budget stands and the non-fit is the recorded outcome).
  - **Scope caveats, so no number here is over-read:** `request.power` was omitted — `sg13cmos5l_stdcell` has no verified tap/endcap masters in klt at this pin, so the floorplan has **no PDN straps, taps, or fill** (the response's `power` block records the absence; a real implementation only grows from here). The `cts` and `route` stages were **not** run: klt's per-library reference tables (`_CTS_BUFFER_CELLS`/`_ROUTING_LAYER_RANGE`/`_ANTENNA_DIODE_CELLS`) have no `sg13cmos5l_stdcell` entry at this pin — a genuine klayout-tools capability gap for this PDK, recorded here rather than worked around, and the reason this record claims a **floorplan/placement** area figure, which is exactly what issue #21's dependency note asked for ("a P&R run that gets as far as a floorplan"). The placement-stage timing figures the responses carry (worst setup slack +17.0 ns at 20 ns for the memory, `timing_status: "constrained"`, pre-route estimated parasitics) are incidental toolchain observations, **not** row-4 evidence, per the sta-corner-sweep record's own stub-scoping precedent.
- **klt provenance**: klt `0.5.0+g1d964cf4ed93` drove every leg (synthesize × 2, place-and-route × 2, pdk find). Engine: `openroad` via the docker wrapper → `openroad/orfs:latest`, container OpenROAD `26Q3-2056-g41a28926b9` (the responses' `engine_version`), klayout `0.30.12`. Provisioning route (recorded because it matters to reproducing the pin): wheel built at the pinned commit from a clean `git worktree add` of `1d964cf4`, installed under the repo's gitignored `.venv/`, driven via `KLT_BIN` — the same route and caveat as `layout/toolchain.json`'s 2026-09-21 paragraph (`uv tool install`'s silent stale-serve on the record host, unreduced).
- **Links**: driver: `flow/run-die-area-par.sh`; recipes: `flow/synthesize-program-memory.json`, `flow/synthesize-protocol-emulator.json`; designs under test: `rtl/protocol_program_memory.v`, `src/tt_um_2amlogic_protocol_emulator.v`; toolchain pin: `layout/toolchain.json` (2026-09-22 paragraph); row-7 status note: `spec/gap-to-submission.md` row 7; tiles statement: `info.yaml`; artifacts: `artifacts/20260922-012402-420c370/` (per-design synthesize + P&R request/response pairs, both frozen netlists, and the run summary `die-area-par-results.json`).
- **Timestamp / author**: 2026-09-22T01:24:02Z, Loom Builder (agent), issue #21.
- **Supersedes**: none.
