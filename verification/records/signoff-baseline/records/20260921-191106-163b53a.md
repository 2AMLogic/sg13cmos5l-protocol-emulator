<!-- record-meta
{
  "record_id": "20260921-191106-163b53a",
  "experiment": "signoff-baseline",
  "supersedes": "20260921-182522-2985c8d",
  "git_revision": "163b53a84ac3c86a7d02b2106aa0eb6e4dfbd0bc",
  "provenance": {
    "klt_version": "0.5.0+g1d964cf4ed93 (the bumped `layout/toolchain.json` pin, installed for this run from a wheel built at commit 1d964cf4ed93b15bd36acf1eb14e3855b3c500d1 under the repo-local gitignored .venv/ via `KLT_BIN` — the pinned-commit build; `klt version --format json` preserved verbatim at artifacts/20260921-191106-163b53a/klt-version.json, and `scripts/signoff-report.sh`'s pin check enforces `git_commit == layout/toolchain.json's klt_last_verified_commit`)",
    "pdk": { "name": "n/a -- `klt signoff --manifest` grades a committed evidence state, no PDK is read", "version": "n/a" },
    "deck": { "content_hash": "n/a -- no deck is graded in a tier-verdict render" },
    "inputs": [
      { "path": "manifests/sg13cmos5l-protocol-emulator.json", "content_hash": "sha256:070ec49594f6d88eea0f13e02f85afdb04168cd164e0b0c7b7ce677726a111f1" },
      { "path": "manifests/design-evidence-tiers.md", "content_hash": "sha256:275964ed6cdc3ed57566710540daa76beb26c1b67fb4b3dbf598d05b640a7ce0" },
      { "path": "verification/records/reset-pin-through/artifacts/20260914-031931-7e4a21a/klt-functional-verification.json", "content_hash": "sha256:c92c5811612a43971d72898a319e36b076b0c3900452bd162cb3da4409453d27" }
    ]
  }
}
-->

- **Record ID**: 20260921-191106-163b53a
- **Claim**: Supersedes `20260921-182522-2985c8d` — the re-rendered `klt signoff` tier-verdict report for the same block manifest `manifests/sg13cmos5l-protocol-emulator.json` (`block: sg13cmos5l-protocol-emulator`, `kind: digital`) at the **bumped** `layout/toolchain.json` pin (issue #20's klt bump, `5c757081680e` → `1d964cf4ed93`), which moves the grader past the very commits that changed the report format (`klayout-tools#2195`/`#2201` — the pin's own HEAD is #2201's per-item `graded_by_build` coverage field). The superseded record's committed artifact was rendered by the pre-bump grader and is byte-stale against the new pin — this `--check-latest` drift is exactly why the pin bump and this re-render land in the same change: the CI re-run job re-renders at the pin and byte-compares against the newest record, so both must move together. Its own Result bullet predicted precisely what this record now confirms empirically: an upstream-main grader renders item 7 `not_post_layout` and both cited items `unverifiable_provenance`.
- **Design provenance**: n/a — this record grades a committed *evidence state* (same three inputs, byte-identical to the superseded record's — the manifests, the vendored tiers doc, and the one cited functional-verification artifact are unchanged by the pin bump), not a design artifact.
- **Run configuration**: `KLT_BIN=.venv/bin/klt scripts/signoff-report.sh --out <report>` from the repo root — the same pinned invocation as the superseded record (`klt signoff --manifest manifests/sg13cmos5l-protocol-emulator.json --format json --tiers-doc manifests/design-evidence-tiers.md`), graded by the pinned-commit build (see `layout/toolchain.json`'s 2026-09-21 paragraph for why this run used a wheel built from a clean `git worktree add` of the pin rather than the user-level `uv tool install`, which silently served the pre-bump tree on this host).
- **Statistical convention**: N/A — deterministic grading of committed evidence.
- **Result**: **`tier: null` — 0 of 11 T1 items met**, `klt signoff` exit 3 (the documented "ran, at least one item unmet" gap state). Substantively unchanged from the superseded record — no item's verdict changed, only the renderer's granularity: items **1, 2, 3, 4, 5, 6, 8, 10, 11**: `unmet`/`no_evidence` (item 5 is STA — issue #20's first increment wired the harness this record's re-run gate now exercises, but no STA evidence citation exists to grade yet, so its verdict is correctly still `unmet`); item **7**: `unmet`/`not_post_layout` (the newer grader now classifies the cited zero-delay functional run exactly as predicted — an SDF-annotated re-verification remains issue #26's scope); item **9**: `unmet`/`unverifiable_provenance`. Every T1 item now carries `graded_by_build: true` (#2201's build-coverage field — a new, more granular "why" alongside unchanged verdicts).
- **klt provenance**: `klt` 0.5.0+g1d964cf4ed93 at `layout/toolchain.json`'s pin `1d964cf4ed93b15bd36acf1eb14e3855b3c500d1`, verified pre-grade by the runner's pin check against `klt version --format json`'s `git_commit` (full payload at `artifacts/20260921-191106-163b53a/klt-version.json`). Unlike the superseded record's grader, this pin's report **does** embed a `build` block — grader identity only (version/package_version/git_commit/git_tag/dirty/is_release; no host paths, timestamps, or klayout version), so the committed report stays byte-reproducible on the pinned dependency graph alone; CI provisions the same pinned commit via `scripts/setup-env.sh` on a clean runner.
- **Links**: block manifest: `manifests/sg13cmos5l-protocol-emulator.json`; graded checklist: `manifests/design-evidence-tiers.md`; runner: `scripts/signoff-report.sh`; CI re-run gate: `.github/workflows/ci.yml` `klt signoff manifest re-run` job; toolchain pin (and the bump's caveat): `layout/toolchain.json`; cited evidence record: `verification/records/reset-pin-through/records/20260914-031931-7e4a21a.md`; tracker: issue #27; superseded record: `records/20260921-182522-2985c8d.md` (stays as frozen history); this record's artifacts: `artifacts/20260921-191106-163b53a/signoff-report.json` (the new verdict of record) and `artifacts/20260921-191106-163b53a/klt-version.json` (grader identity).
- **Timestamp / author**: 2026-09-21T19:11:06Z, Loom Doctor (agent), issue #20 / PR #31.
- **Supersedes**: **`20260921-182522-2985c8d`** — same inputs and same verdicts; superseded solely because the pin bump changed the grader's report format (its per-item reasons and the new `graded_by_build` field), not because any graded judgment changed.
