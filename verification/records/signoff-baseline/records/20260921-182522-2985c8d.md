<!-- record-meta
{
  "record_id": "20260921-182522-2985c8d",
  "experiment": "signoff-baseline",
  "supersedes": null,
  "git_revision": "2985c8d5a6609c3a59de37060c40206637907232",
  "provenance": {
    "klt_version": "0.4.0+g5c757081680e.dirty (isolated `uv tool install` of `layout/toolchain.json`'s `klt_install` pin `git+https://github.com/2AMLogic/klayout-tools@5c757081680e207b6abbc7a6ca7d3688ad07072a`), `klt version --format json` preserved verbatim in this record's artifacts; `scripts/signoff-report.sh`'s pin check enforces `git_commit == layout/toolchain.json's klt_last_verified_commit` and refuses the host `PATH` `klt` (a drifting v0.5.0 release, commit 6bf5610) before grading",
    "pdk": { "name": "n/a -- `klt signoff --manifest` grades a committed evidence state, no PDK is read", "version": "n/a" },
    "deck": { "content_hash": "n/a -- no deck is graded in a tier-verdict render" },
    "inputs": [
      { "path": "manifests/sg13cmos5l-protocol-emulator.json", "content_hash": "sha256:070ec49594f6d88eea0f13e02f85afdb04168cd164e0b0c7b7ce677726a111f1" },
      { "path": "manifests/design-evidence-tiers.md", "content_hash": "sha256:275964ed6cdc3ed57566710540daa76beb26c1b67fb4b3dbf598d05b640a7ce0" },
      { "path": "verification/records/reset-pin-through/artifacts/20260914-031931-7e4a21a/klt-functional-verification.json", "content_hash": "sha256:c92c5811612a43971d72898a319e36b076b0c3900452bd162cb3da4409453d27" }
    ]
  }
}
-->

- **Record ID**: 20260921-182522-2985c8d
- **Claim**: This is the block's first mechanically-rendered `klt signoff` tier-verdict report (issue #16) — the baseline grading of this block's T1 evidence state from the committed block manifest `manifests/sg13cmos5l-protocol-emulator.json` (`block: sg13cmos5l-protocol-emulator`, `kind: digital`), against the vendored eleven-item `klayout-tools` checklist `manifests/design-evidence-tiers.md`. This is the first block manifest committed in the canary fleet per the fleet roll-up's own dependency survey (`2AMLogic/2am`#956, 2026-09-20: "Per-repo block manifests do not exist yet — a companion issue is filed in each canary"; this repo's companion issue is #16, and this record is that issue's committed evidence). **The committed report is the verdict of record for this block's per-item T1 state** — the gap-to-T1 tracker (issue #27) points at it and does not re-derive grades by hand.
- **Design provenance**: n/a — this record grades a committed *evidence state*, not a design artifact; the only design behind a cited artifact is the harness-bootstrap stub top (`src/tt_um_2amlogic_protocol_emulator.v`) via cited evidence record `20260914-031931-7e4a21a` (`verification/records/reset-pin-through/`).
- **Run configuration**: `scripts/signoff-report.sh --out <report>` from the repo root (the runner owns the working directory because manifest evidence paths are cwd-relative), which grades with the pinned invocation `klt signoff --manifest manifests/sg13cmos5l-protocol-emulator.json --format json --tiers-doc manifests/design-evidence-tiers.md`. The `--tiers-doc` override is required to render all eleven checklist items: the vendored doc is `2AMLogic/klayout-tools`@`428951e03693` (the commit that added T1 item 11, power-delivery, per `klayout-tools`#2025/#2057), while the pinned `klt`'s own bundled doc predates that item and lists only ten. Item 11 renders `unmet`/`no_evidence` — an uncited item never falls through to grading in any build, so the pinned grader's lack of item-11 grading rules cannot mis-grade it (see `manifests/README.md`).
- **Statistical convention**: N/A — the render is deterministic grading of committed evidence (same manifests, same pinned grader, same doc bytes, same report); there is no measurement, seed, or randomness in it.
- **Result**: **`tier: null` — 0 of 11 T1 items met** (an all-`unmet` render is a correct result for this repo's current state, exactly as issue #16's own text says). `klt signoff` exited **3**, which per `klt`'s documented contract means "ran successfully, but at least one T1 item is `unmet`" — the normal gap state, **not** an error; gate on the JSON's `tier`/per-item fields, not the exit code (the contract call is quoted in `manifests/README.md`). Per item:
  - items **1, 2, 3, 4, 6, 8, 10, 11**: `unmet`/`no_evidence` — no evidence citation exists; each unmet item's substance and the issue that would close it are tracked in issue #27's checklist rows (RTL #18/#19, layout/DRC/LVS downstream, STA #20 + ratification #17 behind item 5, characterization #21 behind item 8, power-delivery item 11 behind a future P&R). **Item 6 is uncited by explicit statement, not omission**: this block's spec table (`spec/target-spec.md`) has no statistical row (all deterministic protocol-timing/area/pin-budget bounds), satisfying the checklist's own "must say so explicitly rather than omitting the item" clause — the statement lives here, in `manifests/README.md`, and in issue #27's item-6 row.
  - items **7, 9**: `unmet`/`unrecognized_envelope` — both cite the one committed `klt` evidence envelope this repo has (record `20260914-031931-7e4a21a`'s `klt-functional-verification.json`, the stub top's passing zero-delay Icarus/cocotb run), each pinned to the cited artifact's exact bytes (`content_hash` = `sha256:c92c5811…`, verifiable with `sha256sum`). Under the pinned `klt` (which predates `klt signoff`'s functional-verification classification, `klayout-tools`#1959/#1967) the citation resolves but cannot be classified; item 7 is `unmet` on the substance anyway (the cited run is zero-delay, `environment.sdf: null` — an SDF-annotated re-verification is issue #26), and item 9's cited testbench is the harness half of that item with the ISA-specific benches (#22/#23/#24) the unmet remainder. The verdict is **`unmet` under every grader version** — pinned build, current release, or current upstream `main` (which would render item 7 `not_post_layout` and both `unverifiable_provenance`, per the manifest's pin discipline; see `manifests/README.md`) — never a false green.
- **klt provenance**: `klt` 0.4.0+g5c757081680e.dirty (`2AMLogic/klayout-tools` @ `5c757081680e207b6abbc7a6ca7d3688ad07072a` — the exact `layout/toolchain.json` pin, verified pre-grade by `scripts/signoff-report.sh`'s pin check against `klt version --format json`'s `git_commit`; full version payload preserved at `artifacts/20260921-182522-2985c8d/klt-version.json`). The grading binary was provisioned for this run as an **isolated** `uv tool` install of the pin (`KLT_BIN` override) so it does not clobber the host-wide `klt`; the host `PATH` `klt` is a drifting v0.5.0 release (commit `6bf5610`) that the runner's pin check deliberately **refuses** — CI (`ci.yml`'s `signoff` job) provisions the same pin via `scripts/setup-env.sh`, and the report bytes compared by `--check-latest` do not embed grader build metadata (the pinned build predates `klt signoff`'s `build` block), so the committed report is reproducible byte-for-byte on the pinpointed dependency graph alone.
- **Links**: block manifest: `manifests/sg13cmos5l-protocol-emulator.json`; graded checklist: `manifests/design-evidence-tiers.md` (vendored copy — provenance and refresh rule in `manifests/README.md`); runner: `scripts/signoff-report.sh`; CI re-run (the "fails rather than rotting" gate): `.github/workflows/ci.yml` `signoff` job; toolchain pin: `layout/toolchain.json`; cited evidence record: `verification/records/reset-pin-through/records/20260914-031931-7e4a21a.md` and its cited artifact `verification/records/reset-pin-through/artifacts/20260914-031931-7e4a21a/klt-functional-verification.json`; tracker (points here as the verdict of record): issue #27; this record's own artifacts: `artifacts/20260921-182522-2985c8d/signoff-report.json` (the rendered tier report — the verdict of record), `artifacts/20260921-182522-2985c8d/klt-version.json` (grader identity).
- **Timestamp / author**: 2026-09-21T18:25:22Z, Loom Builder (agent), issue #16.
- **Supersedes**: none.
