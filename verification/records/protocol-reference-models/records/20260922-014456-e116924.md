<!-- record-meta
{
  "record_id": "20260922-014456-e116924",
  "experiment": "protocol-reference-models",
  "supersedes": null,
  "git_revision": "e116924",
  "provenance": {
    "klt_version": "0.4.0+g5c757081680e.dirty",
    "pdk": { "name": "n/a", "version": "n/a" },
    "deck": { "content_hash": "n/a" },
    "inputs": [
      { "path": "verification/reference_models/__init__.py", "content_hash": "sha256:5ab65f996e564f9be69bb12dcee0b8e0dbbd9fe4768c81747e8be90ca548dfde" },
      { "path": "verification/reference_models/waveform.py", "content_hash": "sha256:e1b521acd3be65db67762afac5dcd51aa0ed22105fa76db2783eaaa46ac0dcd6" },
      { "path": "verification/reference_models/uart.py", "content_hash": "sha256:e6ceb41a3458698c91f61dc339c75871cc650dea99190f13b5e2b26f7d53d851" },
      { "path": "verification/reference_models/spi.py", "content_hash": "sha256:44040fccb308509012757e4525d5749bf2aa802bee21ee982dcb7f1101315b6a" },
      { "path": "verification/reference_models/i2c.py", "content_hash": "sha256:26433d536f517bc86e3cc1525ecdd0422d379cbede99f44b48d8b784d201f836" },
      { "path": "verification/reference_models/stimulus.py", "content_hash": "sha256:dde06d2a41a6bd96ebbf68a45964478c99b1dcda80d70839d6b020a6677484db" },
      { "path": "verification/test_protocol_models.py", "content_hash": "sha256:e7b7681b33176207864cf98bec1e6eecd40c55dcbca4a89663fe4f2e6972605a" },
      { "path": "verification/request-protocol-models.json", "content_hash": "sha256:0551d214ee7eba33824fc0102b7669138cf288873f20b846c9ea3fdd01d0500c" },
      { "path": "verification/duts/model_validation_top.v", "content_hash": "sha256:aff93b25346c2529a9116a61bda63d37bb383b2d235c46351b9edcf90cdde5e7" }
    ]
  }
}
-->

- **Record ID**: 20260922-014456-e116924
- **Claim**: The section-4 (verification-plan.md) **independent reference models** for all three ratified core protocols exist and pass constrained-random validation: a UART model (8N1 encode/decode with measured cumulative per-frame drift against target-spec row 10's ~2%-of-bit-period bound), an SPI model (Motorola/NXP SPI Block Guide conventions, all four (CPOL, CPHA) modes, both data lines re-derived from the mode-correct sample edges, with row 11's SCLK <= f_clk/4 ceiling enforced), and an I2C model (controller-role traffic re-derived from SCL/SDA and checked against NXP UM10204 Rev. 6 Table 10's t_LOW/t_HIGH/t_HD;STA/t_SU;STA/t_SU;STO minimums directly, in both Standard and Fast mode, with repeated-START and peripheral clock-stretching handled). The constrained-random generators sweep the plan's named dimensions per protocol (UART: baud across 9,600-1,000,000 + bytes + receive-side jitter; SPI: modes 0-3, lengths, SCLK across legal dividers, MOSI/MISO patterns; I2C: Std/Fast, addresses in UM10204's valid static range, per-byte ACK/NACK, clock-stretching, repeated START), driven by one recorded seed (`20260922`, in `RECORDED_SEED` and mirrored in the request's `random_seed`), and deterministic negative controls prove each model **flags** the corruption it exists to catch (2.5%-slow UART frame caught by the drift measurement, not the byte decode; f_clk/3 SPI burst caught by the ceiling; mode-0-traffic-judged-as-mode-1 caught by bit-exact comparison; half-minimum t_SU;STO caught by name). **What this record is NOT**: a claim about any RTL. The DUT-facing half of section 4 -- the core running committed firmware, observed through these same models -- cannot exist yet (issue #18 core datapath in flight; issues #22/#23 assembler/firmware not landed); the cocotb toplevel here is the declared fixture `duts/model_validation_top.v`, never driven or checked by the bench. Target-spec rows 1, 10, 11 and 12 therefore remain **unmet**; this record is the models-half evidence `spec/gap-to-submission.md` row 8 now cites.
- **Design provenance**: `verification` (`verification/reference_models/` @ `e116924` -- pure Python, standard library only, no dependency on `src/` or `rtl/`; independence is section 4's requirement and is structural here: the models encode row 10/11/12's cited external conventions, never this repo's DR 0001 cycle-budget arithmetic).
- **Run configuration**: `klt functional-verification verification/request-protocol-models.json --format json` (Icarus Verilog 13.0, cocotb 2.1.0, `random_seed: 20260922` -- the recorded seed this record cites, mechanically tied to the bench by `test_recorded_seed_matches_request`; `timescale: ["1ns","1ps"]`; toplevel `duts/model_validation_top.v`, the declared fixture; 9 test cases: 1 seed-discipline, 3 constrained-random (64 rounds per protocol, one `random.Random(20260922)` per protocol driving case generation and jitter), 1 stretch-legality, 4 negative controls).
- **Statistical convention**: constrained-random with a **recorded seed** -- every case is a pure function of `RECORDED_SEED = 20260922`, so the run is byte-reproducible (`klt`'s own `random_seed` mirrors it; cocotb's RNG is not used by this bench, Python's `random.Random(20260922)` drives everything). 64 rounds per protocol cover the generators' constrained dimensions exhaustively where finite (all 9 bauds of the ladder, all 4 SPI modes, both I2C modes asserted at exit). The negative controls are deterministic (fixed waveforms, not seed-derived), so failure detection fires identically on every run regardless of seed.
- **Result**: **PASS.** All 9/9 test cases passed (0 failed, 0 skipped; per-test statuses in `artifacts/20260922-014456-e116924/results_icarus.xml`): the recorded seed matches the request; random UART frames round-trip at every baud 9,600-1,000,000 with measured per-frame drift inside the 2% bound; the 2.5%-slow negative frame is flagged by drift (20.000% of a bit period at the measured edge) while its byte still decodes correctly -- the case only the timing measurement catches; random SPI bursts pass and round-trip bit-exactly in all four modes across dividers 4-64 at f_clk = 50 MHz (draft row-4 target used as the ceiling's input, not a claim 50 MHz is closed); the f_clk/3 burst is flagged naming the ceiling; mode-0 traffic judged as mode 1 fails to round-trip; random I2C transfers in both modes decode address/RW/data/ACKs exactly and meet every Table-10 minimum (repeated-START cases generated and covered); clock-stretching is accepted and reported (`stretched_clocks >= 1`, zero violations); the half-minimum t_SU;STO STOP is flagged by name.
- **klt provenance**: `klt` 0.4.0+g5c757081680e.dirty (uv git-worktree build tag of the `layout/toolchain.json` pin `5c757081680e207b6abbc7a6ca7d3688ad07072a`, self-reporting as the dirty-suffixed variant the program-load-phase record also observed) drove this run via `klt functional-verification`. No PDK dependency: `pdk`/`deck` are `n/a`, per this convention's rule for Icarus/cocotb-only runs.
- **Links**: models: `verification/reference_models/{__init__,waveform,uart,spi,i2c,stimulus}.py`; driver/testbench: `verification/test_protocol_models.py`; request: `verification/request-protocol-models.json`; fixture toplevel: `verification/duts/model_validation_top.v`; the spec rows this evidence serves: `spec/target-spec.md` rows 1/10/11/12 (unmet) and row 8, `spec/verification-plan.md` section 4, `spec/gap-to-submission.md` row 8; the DUT-facing half these models await: issues #18, #22, #23; artifacts: `artifacts/20260922-014456-e116924/`.
- **Timestamp / author**: 2026-09-22T01:44:56Z, Loom Builder (agent), issue #25.
- **Supersedes**: none.
