<!-- record-meta
{
  "record_id": "20260922-011856-87e8166",
  "experiment": "post-layout-sdf-regression",
  "supersedes": null,
  "git_revision": "420c370",
  "provenance": {
    "klt_version": "0.5.0+g1d964cf4ed93.dirty",
    "pdk": {
      "name": "ihp-sg13cmos5l",
      "version": "unknown (local PDK install reports no version stamp; `klt pdk find --pdk ihp-sg13cmos5l --format json` returns no version field)"
    },
    "deck": {
      "name": "sg13cmos5l_stdcell Verilog timing models (sg13cmos5l_stdcell.v, specify blocks unconditional) + sg13cmos5l_udp.v, from the local PDK install's libs.ref/sg13cmos5l_stdcell/verilog/",
      "content_hash": "sha256:b0adbc84c8f78feb01c3d8d9d55e9818ee6502347d336f3a1f846e6a5d64e0a2 (stdcell) · sha256:5491a3edf7468792b5222e7abe057e0583a407c01b2086d4fd44a55b6cf88055 (udp)"
    },
    "inputs": [
      { "path": "src/tt_um_2amlogic_protocol_emulator.v", "content_hash": "sha256:4945013ad813d88c92b20beb8f959d44080fe7b009d6ca55edd32b34ffd15708" },
      { "path": "verification/test_protocol_emulator.py", "content_hash": "sha256:0cc707de06290f5fc6df4b61521a531fc75e6baab204e6e406a018bc439d682f" },
      { "path": "verification/_dut.py", "content_hash": "sha256:8e89b02ff7bdafac12f1213a3142ec3e8a8ff95ca2a5e5772d271e74040ec0af" },
      { "path": "flow/run-post-layout-sdf.sh", "content_hash": "sha256:44e3170487938a5624a90ad9c54d14e818d3b6c34b10da788f3283e4ce253e73" }
    ]
  }
}
-->

- **Record ID**: 20260922-011856-87e8166
- **Claim**: T1 checklist item 7 (post-layout verification, issue #26) — this program's own committed cocotb bench (`verification/test_protocol_emulator.py`, byte-unmodified, resolved via `testbench.search_path`) re-run against the **post-route gate-level netlist** of the current top with **back-annotated SDF timing**, at every PVT corner SDF the post-route flow emits (`nom_typ_1p20V_25C`, `nom_slow_1p08V_125C`, `nom_fast_1p32V_m40C`), each run a `klt functional-verification` response with `environment.sdf.annotated: true` — the machine-checkable shape `manifests/design-evidence-tiers.md` item 7 accepts and `klt signoff` grades on. The block manifest's item-7 citation now points at this record's typ-corner envelope (`klt-functional-verification-nom_typ_1p20V_25C.json`, content-pinned by the manifest hash). **What this record is NOT**: not ISA-core evidence — the swept netlist is the harness-bootstrap stub top's post-route implementation (8 flops + 8 enable muxes + 37 clock/input/output/tie-path logic cells + 2,303 fill/decap/tie cells, 2,340 instances total), so this closes item 7 *for the current design revision* the way issue #26 scopes it (the evidence leg: "an SDF-annotated regression is what item 7 actually requires"); when issue #18's core integration changes the top, the freshness rule (a passing report against a stale netlist is evidence of nothing) requires re-running this same one-command harness, and `layout/toolchain.json`'s provenance hash on `src/` is the tripwire. It is also not row-4/50 MHz evidence in any direction — timing margin is item 5's STA subject, not this functional re-simulation's.
- **Design provenance**: `netlist` (post-route, LibreLane `gds`-workflow run `35659983709`, 2026-09-21T21:56Z, at `main` @ `420c370994deff79f18849113b0d2a6985d47715` — exactly this record's `git_revision`, i.e. the current tree; frozen verbatim as `artifacts/20260922-011856-87e8166/tt_um_2amlogic_protocol_emulator.v`, sha256 `b3e25723…`) from `rtl` (`src/tt_um_2amlogic_protocol_emulator.v` @ `420c370`, sha256 `4945013a…` — identical to the bytes every prior record of this top cites, so the swept netlist is fresh against the current source). The SDFs are the same run's own `final/sdf/<corner>/` outputs (OpenSTA 2.7.0 `write_sdf` at LibreLane's post-route `stapostpnr` stage), frozen per corner beside the netlist they annotate. Flow attribution per CLAUDE.md's two-flows rule: netlist, SDF, and SPEF-parasitics-derived delays are all **Tiny Tapeout LibreLane CI flow** products; the re-simulation harness (`klt functional-verification`, Icarus, cocotb) is **this program's klt flow** — a deliberate cross-flow leg, since item 7's consumer is the klt evidence record, and the two flows' agreement on the zero-delay gate-level behavior is already cross-checked by the template's own `gl_test` job on the same netlist bytes (run `35659983709`, `gl_test: success`).
- **Run configuration**: `KLT_BIN=.venv/bin/klt ./flow/run-post-layout-sdf.sh --run-id 35659983709` from the repo root (the runner downloads the workflow's `tt_submission` + `GDS_logs` artifacts, freezes the netlist and the three final SDFs, and drives one `klt functional-verification` run per corner). Per corner: engine `icarus` (Icarus Verilog 13.0, cocotb 2.1.0, `random_seed: 1`, `timescale: ["1ns","1ps"]`), `sources` = the frozen routed netlist + the PDK's `sg13cmos5l_stdcell.v` + `sg13cmos5l_udp.v` (no `FUNCTIONAL` define — the IHP models carry their specify blocks unconditionally, which is what makes them SDF-annotatable), `hdl_toplevel: tt_um_2amlogic_protocol_emulator`, `options.sdf: {file: <corner SDF>, corner: "typ"}` (`iverilog -T typ` picks the typ member of every min:typ:max triplet). **SDF normalization, disclosed and mechanically audited** (per-corner `*.normalization.json` reports, original and normalized bytes both frozen): (1) the three header triplets OpenSTA writes as `min::max` (`VOLTAGE`/`PROCESS`/`TEMPERATURE`, empty typ member — already filed upstream as `2AMLogic/klayout-tools#1880`) get the typ member filled, documentary header fields with no delay content; (2) all-zero-delay `INTERCONNECT` entries are dropped — 42 (fast), 43 (slow), 42 (typ) per corner — because Icarus cannot insert an intermodpath whose top-level-port endpoint is driven through a netlist `assign` alias (`SDF ERROR: Could not find intermodpath!`, the exact shape inserted tie cells on constant outputs produce; filed upstream as `2AMLogic/klayout-tools#2285`), and an all-zero entry models no delay, so dropping it cannot change any simulated timing. Every nonzero IOPATH and INTERCONNECT entry annotated with zero `SDF WARNING`/`SDF ERROR` lines in every transcript (klt's diagnostic gate enforces this; it is what makes `annotated: true` trustworthy). klt's own envelope additionally discloses `partial: true` with `dropped.timingcheck.count: 8` per corner — Icarus implements SDF delays but not SDF TIMINGCHECK sections, a documented engine limitation ($setup/$hold reporting stays STA's job, item 5's subject).
- **Statistical convention**: N/A — directed tests only (3 test cases), no randomization beyond the pinned `random_seed: 1`; one deterministic run per corner over one deterministic netlist and three per-corner SDFs.
- **Result**: **PASS at all three corners** — each run `status: "pass"`, 3/3 tests (`test_reset_clears_output`, `test_pin_through`, `test_ena_gates_updates`) passed, 0 failed, 0 skipped, `environment.sdf` an object with `annotated: true` (versus `null` on the zero-delay runs — the field that makes the two unmistakable). **Finding watch (issue #26's last checklist item): no test that passes zero-delay fails annotated** — all three tests pass identically under the zero-delay RTL run (record `20260914-031931-7e4a21a`), the template's zero-delay gate-level `gl_test`, and every annotated corner here; the register-capture bench settles over 2 cycles by design, so ~0.001–0.01 ns post-route net delays and ~0.2 ns clock-to-Q cell delays (per the frozen SDFs) are invisible to it at the bench's own 10 ns clock — the annotated run's value is that it *demonstrates* the annotation path end to end (every delay entry applies, no silent fallback), not that it stressed setup margin. Raw envelopes: `klt-functional-verification-<corner>.json` + `results_icarus-<corner>.xml` per corner; sweep summary: `sweep-summary.json`.
- **klt provenance**: klt `0.5.0+g1d964cf4ed93.dirty` (`2AMLogic/klayout-tools` @ `1d964cf4ed93b15bd36acf1eb14e3855b3c500d1`, `layout/toolchain.json`'s current pin, carrying the same `.dirty` build-identity bit CI's `uv tool install` route produces) drove every leg — artifact acquisition aside, each corner is one `klt functional-verification` invocation. Provisioned per the pin's recorded host caveat: a wheel built at the pinned commit installed under this worktree's gitignored `.venv/` and invoked via `KLT_BIN` (the route `layout/toolchain.json`'s 2026-09-21 paragraph documents; `uv tool install` served a stale tree on this host across three attempts). The SDF-annotation subsystem used here (`options.sdf`, the `klt_sdf_dut_wrapper`/`klt_sdf_annotate` elaboration roots, the safe/deferred vector-port split, and the fail-on-any-diagnostic transcript gate) is that pin's implementation of klayout-tools #962/#1002/#1056/#1619/#2130.
- **Links**: driver: `flow/run-post-layout-sdf.sh`; bench (unmodified): `verification/test_protocol_emulator.py` + `verification/_dut.py`; design under test: `src/tt_um_2amlogic_protocol_emulator.v`; routed artifacts: gds run [`35659983709`](https://github.com/2AMLogic/sg13cmos5l-protocol-emulator/actions/runs/35659983709) (`gds-run-commit-id.json` frozen); manifest citation this record backs: `manifests/sg13cmos5l-protocol-emulator.json` item 7; upstream tool-gap filings: `2AMLogic/klayout-tools#1880` (header `min::max` triplets, pre-existing) and `2AMLogic/klayout-tools#2285` (assign-alias intermodpath, filed from this run); tracker row: `spec/gap-to-submission.md` row 8; artifacts: `artifacts/20260922-011856-87e8166/` (netlist, 3× original + normalized SDFs with normalization reports, 3× request/response/console/JUnit, sweep summary, gds-run commit id).
- **Timestamp / author**: 2026-09-22T01:18:56Z, Loom Builder (agent), issue #26.
- **Supersedes**: none.
