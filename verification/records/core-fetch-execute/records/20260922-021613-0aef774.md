<!-- record-meta
{
  "record_id": "20260922-021613-0aef774",
  "experiment": "core-fetch-execute",
  "supersedes": null,
  "git_revision": "0aef774437f1a3142e2e9e1c024534ff781f53da",
  "provenance": {
    "klt_version": "0.5.0+g1d964cf4ed93",
    "pdk": { "name": "ihp-sg13cmos5l", "version": "unknown (local PDK install reports no version stamp - `klt pdk find --pdk ihp-sg13cmos5l` reports no version)" },
    "deck": { "content_hash": "n/a" },
    "inputs": [
      { "path": "src/tt_um_2amlogic_protocol_emulator.v", "content_hash": "sha256:8401ce2cf6582cfd3a5cb2d61128517dddfc8641e928f40cc906211b0e297d30" },
      { "path": "rtl/protocol_core.v", "content_hash": "sha256:e26a0f8babbb62252b9e3d61e2e06368c9054cde4fae41ab99c87abce02b613f" },
      { "path": "rtl/protocol_program_memory.v", "content_hash": "sha256:c7f989cfff1d45f40c110ffa4a29aae8c0bd876be47af943b6e53001cb38e4d1" },
      { "path": "verification/test_protocol_emulator.py", "content_hash": "sha256:017e46780ddfa03d35ea7d77d063eb5f2f590f03b5165ca052136ad13d735c99" },
      { "path": "verification/request-protocol-emulator.json", "content_hash": "sha256:5f0751c894db8c363fc51449952a043d7f6e25e71bd5721be633ffd51636cb0e" }
    ]
  }
}
-->

- **Record ID**: 20260922-021613-0aef774
- **Claim**: issue #18's acceptance criterion "cocotb bench in `verification/` exercising fetch/execute for every instruction class the ratified ISA defines": `rtl/protocol_core.v` (wired into the top by this issue, per `spec/decision-records/0001-isa.md`) executes every one of the 16 opcodes -- NOP, LDI, MOV, ADD, SUB, AND, OR, XOR, SHF, IN, OUT, WAIT, JMP, BZ, BNZ, HALT -- as real firmware loaded over the serial load-phase protocol through the actual pins (MODE = `ui_in[7]`, serial = `ui_in[0]`), checked at pin observables and cycle-exactly where DR 0001 makes a cycle claim: per-instruction retirement edges (1 cycle for every opcode but WAIT), OUT pin latency (new value visible exactly one cycle after the retiring OUT, and not before -- "Drive on edge"), IN sampling at the retiring edge ("Sample on edge"), WAIT imm8+1 stalls (including the WAIT 0 corner), taken and not-taken branches costing the same 1 cycle, the 3-cycle-per-iteration countdown loop shape DR 0001's UART low-baud budget is expressed in, and a bench-level shadow of the row-3 no-data-dependent-latency property (identical-shape programs with flag-differing ALU data retire their markers on identical edges; the formal property itself remains its own tracked row-3 item). Direction no-ops are pinned too: IN to a write-only port and OUT to a read-only port each leave state untouched while still costing 1 cycle, and `OUT` to port 11 never moves `uio_oe` (direction fixed at synthesis time).
- **Design provenance**: `rtl` (`src/tt_um_2amlogic_protocol_emulator.v` + `rtl/protocol_core.v` + `rtl/protocol_program_memory.v` @ `0aef774`; the top instantiates both modules and the bench drives only the top's pins -- the same load-then-run path real firmware for this chip uses, no backdoor).
- **Run configuration**: `KLT_BIN=.venv/bin/klt klt functional-verification verification/request-protocol-emulator.json --format json` (klt `0.5.0+g1d964cf4ed93` at the `layout/toolchain.json` pin, Icarus Verilog 13.0, cocotb 2.1.0, `random_seed: 1`, `timescale: ["1ns","1ps"]`, a 10 ns simulation clock, 12 directed test cases). The pinned klt ran from a wheel built at the pinned commit installed under the worktree's gitignored `.venv/` (the `uv tool install` stale-serve host caveat in `layout/toolchain.json`'s 2026-09-21 paragraph still applies; same provisioning route as the sta-corner-sweep record).
- **Statistical convention**: N/A -- directed tests only. Every program and data pattern is fixed in source (`verification/test_protocol_emulator.py`'s `enc()` assembler and per-test program listings), so every run is byte-reproducible. The committed test programs are part of the evidence byte-for-byte per CLAUDE.md's "firmware is evidence".
- **Result**: **PASS.** All 12/12 directed tests passed (0 failed, 0 skipped; per-test statuses in `artifacts/20260922-021613-0aef774/results_icarus.xml`, full envelope in `klt-functional-verification.json`): reset-through-load leaves both pin-output registers cleared and `uio_oe` at 0, HALT freezes the core; LDI/MOV/OUT show the one-cycle write latency with the pre-edge pin still at its old value (no combinational leak); ADD/SUB/AND/OR/XOR results on the pin from independent operands (0xF9/0xAD/0x02/0xF7/0xF5 for 0x53 op 0xA6); Z/C flag semantics per opcode read white-box inside the core (C is write-only state in this ISA revision -- no instruction reads it -- and SUB's C is pinned to the borrow convention the 9-bit subtract produces, documented in `rtl/protocol_core.v`'s header); SHF direction/results; IN ports 00/01 sampling and both direction no-ops; OUT port 11 driving `uio_out` without ever moving `uio_oe`; WAIT 0/1/5 at exactly imm8+1 cycles by retirement-edge arithmetic; BZ taken / JMP / BNZ not-taken each costing exactly 1 cycle; the countdown loop at exactly 3 cycles per iteration for 3 iterations plus fall-through; NOP at 1 cycle; and the two data-differing identical-shape programs landing their markers on identical edges. Cycle-exactness is asserted against `retire_edge()` -- instruction-position arithmetic computed from DR 0001's per-opcode cycle table alone -- so any hidden stall (a data-dependent latency) fails the edge expectations by construction.
- **klt provenance**: klt `0.5.0+g1d964cf4ed93` drove the run (Icarus engine; engine and cocotb versions echoed in the frozen envelope's `environment` block). The PDK install reports no version stamp (`klt pdk find` resolves `~/share/pdk`, version `-`), same finding as the program-load-phase and sta-corner-sweep records; this run has no PDK dependency (Icarus-only cocotb, never touches a standard-cell library), so `deck.content_hash` is `"n/a"` per `verification/README.md`.
- **Links**: driver/testbench: `verification/test_protocol_emulator.py`; request: `verification/request-protocol-emulator.json`; design under test: `src/tt_um_2amlogic_protocol_emulator.v`, `rtl/protocol_core.v`, `rtl/protocol_program_memory.v`; the authoritative ISA contract: `spec/decision-records/0001-isa.md`; the load-phase module record: `verification/records/program-load-phase/`; the template's own harness checking the same top end to end: `test/test.py` (a 3-instruction firmware echo); artifacts: `artifacts/20260922-021613-0aef774/`.
- **Timestamp / author**: 2026-09-22T02:16:13Z, Loom Builder (agent), issue #18.
- **Supersedes**: none.
