<!-- record-meta
{
  "record_id": "20260921-185910-248bf61",
  "experiment": "program-load-phase",
  "supersedes": null,
  "git_revision": "248bf61",
  "provenance": {
    "klt_version": "0.4.0+g5c757081680e.dirty",
    "pdk": { "name": "ihp-sg13cmos5l", "version": "unknown (local PDK install reports no version stamp - `klt pdk find --pdk ihp-sg13cmos5l` reports no version)" },
    "deck": { "content_hash": "sha256:34163f3e7de9f854afac57332ba06f1739b78e7ac35660a8a5ddd39c570124ad" },
    "inputs": [
      { "path": "rtl/protocol_program_memory.v", "content_hash": "sha256:c7f989cfff1d45f40c110ffa4a29aae8c0bd876be47af943b6e53001cb38e4d1" },
      { "path": "verification/test_program_memory.py", "content_hash": "sha256:1df0efe9b6b801e45a417da07091808c2c7d5789b1ef1f57ecb176de0e3849fa" },
      { "path": "verification/request-program-memory.json", "content_hash": "sha256:e837edc016b289f4634d3caf0b0f2df7a6bb088749b9acb4055c377923112a6a" },
      { "path": "flow/synthesize-program-memory.json", "content_hash": "sha256:1027d597f33e037767070c97e9964b512f219ee7d80a2ed8883cebecc256b6db" },
      { "path": "flow/run_synthesize_direct_yosys.py", "content_hash": "sha256:231b86cbc66135d2ea57e999f436dc516075d02c70818b704a6211e26e6c0712" }
    ]
  }
}
-->

- **Record ID**: 20260921-185910-248bf61
- **Claim**: `rtl/protocol_program_memory.v` (issue #19, target-spec row 6) implements DR 0001 section "Program memory sizing and loading": a 256x16-bit flip-flop program memory addressed by an 8-bit PC, serially loaded by the reset-gated load phase -- MODE (`ui_in[7]`) latched only at the first clock edge after `rst_n` release, one bit per clock edge shifted MSB-first from `ui_in[0]`, one program word committed on its 16th bit's own clock edge at an auto-incrementing write address starting at 0, MODE dropping low ending the load phase (discarding any partial word, saturating at 256 words without wraparound, never re-entering load mid-run without a fresh reset), and the same-cycle combinational fetch port the core's single-cycle fetch-and-execute will drive. `verification/test_program_memory.py` is this row's "Checked by" cocotb load-phase test: it loads known programs serially and reads every word back through the fetch port, proving the bits landed where the ISA says they should.
- **Design provenance**: `rtl` (`rtl/protocol_program_memory.v` @ `248bf61`; see `rtl/README.md` -- the module lives under `rtl/` because only the Tiny Tapeout top is pinned to `src/` by `info.yaml`).
- **Run configuration**: `klt functional-verification verification/request-program-memory.json --format json` (the pristine-install klt at the `layout/toolchain.json` pin, Icarus Verilog 13.0, cocotb 2.1.0, `random_seed: 1`, `timescale: ["1ns","1ps"]`, a 10 ns simulation clock, all 6 directed test cases). Synthesis leg (supporting data, iteration flow): `klt synthesize flow/synthesize-program-memory.json --pdk ihp-sg13cmos5l --format json` attempted at the same pinned klt and failed with the recorded klayout-tools#1786 liberty-naming error (raw error frozen in `artifacts/20260921-185910-248bf61/klt-synthesize-error.json`, byte-identical failure shape to `verification/records/synthesis-baseline/`); `python3 flow/run_synthesize_direct_yosys.py flow/synthesize-program-memory.json --pdk-root /Users/rwalters/share/pdk --out-dir <scratch>` (the documented stopgap - Yosys 0.69+post, liberty `sg13cmos5l_stdcell_typ_1p20V_25C.lib`) drove the recorded synthesis result instead.
- **Statistical convention**: N/A -- directed tests only. All program patterns (the 16-bit LCG sequence and the fixed constants) are generated by fixed-in-source code with no randomization, so every run of this bench is byte-reproducible; the full 256-word program's pattern function is printed in `verification/test_program_memory.py` (`lcg_words`).
- **Result**: **PASS.** All 6/6 directed tests passed (0 failed, 0 skipped; per-test statuses in `artifacts/20260921-185910-248bf61/results_icarus.xml`): mode-low-at-release enters run immediately; a single word 0x1234 loads MSB-first and survives a MODE drop on the edge immediately after its 16th bit (commit-on-16th-bit-edge pinned); a full 256-word program reads back exactly through the fetch port at every address 0..255; a mid-word MODE drop commits only the complete words and leaves a canary word intact while the next reset realigns the writer to address 0; a reprogram attempt during run phase (MODE high again, a full word streamed) writes nothing; overflow streaming past 256 words saturates without wrapping over the loaded program. Load timing is asserted **cycle-exact** at the functional level: a full 256-word load spans exactly 4097 clock edges from the mode-sampling edge to the exit edge (1 sampling + 16*256 bit edges + 1 MODE-drop), i.e. 16 cycles per word -- 81.94 us for the full load *by arithmetic at the draft 50 MHz target* (`info.yaml`'s unratified `clock_hz`). **No silicon clock is claimed by this record**: no STA run exists on either flow for this cell library (gap row 4 remains open on both flows; the newer local klt build used as corroboration below reports `timing: null` for `sg13cmos5l_stdcell`, "no verified mapping for ABC timing"), so target-spec row 6 is not claimed at any demonstrated clock -- per this issue's acceptance criterion, a row not claimed at a clock the design has not demonstrated. Supporting iteration-flow synthesis of the module: 11,603 cells, area 312,062.8896 (liberty area units), 4,127 flip-flops (`sg13cmos5l_dfrbpq_1`) -- 4,096 of them the program-memory bits, the rest the 16-bit shift register, the 8-bit write address, the bit counter and phase state; no latches inferred (Yosys `stat` + structural checks clean). The FF-based memory is DR 0001's non-stretch design; the row 6 stretch item (a Tiny Tapeout SRAM macro instead of flip-flops, if area allows) remains untaken, flagged in `spec/gap-to-submission.md` row 6's note because this cell count is the concrete input that decision will weigh.
- **klt provenance**: The functional-verification leg was driven by the `layout/toolchain.json`-pinned klt, installed exactly as `scripts/setup-env.sh` provisions it (`uv tool install --force --with cocotb==2.1.0 --with cocotb-tools==0.1.0 "klayout-tools @ 5c757081680e207b6abbc7a6ca7d3688ad07072a"`); the binary self-reports `0.4.0+g5c757081680e.dirty` (uv's git-worktree build tag; the dist-info `direct_url.json` names requested commit `5c757081680e207b6abbc7a6ca7d3688ad07072a`), matching the pin `verification/records/reset-pin-through/`'s record cites as `0.4.0+g5c757081680e`. `klt synthesize` at this pin still cannot resolve the liberty (`klayout_tools.synthesize._resolve_liberty` hardcodes `<cell_library>__<corner>.lib`, double underscore, while this PDK ships `sg13cmos5l_stdcell_typ_1p20V_25C.lib`, single underscore -- `2AMLogic/klayout-tools#1786`, unchanged since the synthesis-baseline record), so the stopgap drove the synthesis leg. **Corroboration (a flow-parity finding, recorded per CLAUDE.md/DR 0002 rather than silently resolved):** an unpinned, locally-built klt (`0.5.0+g1d964cf4ed93.dirty`, a commit that postdates the pin and demonstrably resolves the single-underscore liberty) ran `klt synthesize` end to end on this same recipe and its results -- 11,603 instances, area 312,062.8896, identical cell histogram (frozen in `artifacts/20260921-185910-248bf61/klt-synthesize-corroboration-klt0.5.0-g1d964cf.json`) -- agree exactly with the stopgap's. That build is NOT this repo's pin and is not treated as a re-pin (the toolchain pin changes by its own append-only record discipline, not by this issue's evidence); the agreement is what the two-flows rule makes evidence about flow parity here. The PDK install reports no version stamp (`klt pdk find` resolves `~/share/pdk`, version `-`), same as the synthesis-baseline record's finding; the liberty's content hash matches that record's `deck.content_hash` exactly (`sha256:34163f3e...`).
- **Links**: driver/testbench: `verification/test_program_memory.py`; request: `verification/request-program-memory.json`; design under test: `rtl/protocol_program_memory.v`; synthesis recipe: `flow/synthesize-program-memory.json`; synthesis stopgap driver: `flow/run_synthesize_direct_yosys.py`; the load-protocol pin roles in the datasheet: `info.yaml` (`ui[0]=PROG_SER`, `ui[7]=PROG_MODE`); the core-integration contract for #18: `rtl/README.md` and the module header's "Interface intent"; the authoritative behavior contract: `spec/decision-records/0001-isa.md` section "Program memory sizing and loading"; artifacts: `artifacts/20260921-185910-248bf61/`.
- **Timestamp / author**: 2026-09-21T18:59:10Z, Loom Builder (agent), issue #19.
- **Supersedes**: none.
